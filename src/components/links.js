import React from "react";

function Links(links) {
    return (
        <div className="Links">
            <h3>Links</h3>
            <a href={links.github}>{links.github}</a>
            <a href={links.github}>{links.github}</a>
        </div>
    )
}
export default Links;